[pytest]
minversion = 7.0
addopts = -ra -q --tb=short
testpaths =
    tests
markers =
    unit: marks tests as isolated unit tests
    gpu: marks tests that require CUDA
    evolution: marks tests specifically for the TensorNEAT PyTorch port